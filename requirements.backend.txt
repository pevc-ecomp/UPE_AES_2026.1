# Todas as versões são pinadas com == (builds reproduzíveis).
# Apenas dependências importadas diretamente pelo código do backend.

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0

# ── LLM ───────────────────────────────────────────────────────────────────────
langchain-core==1.4.0
langchain-ollama==1.1.0
anthropic==0.116.0

# ── Banco de dados ────────────────────────────────────────────────────────────
sqlmodel==0.0.38
sqlalchemy==2.0.50

# ── Configuração e validação ──────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
